import sidebar from './sidebar';
import { NavItemType } from 'types';

// ==============================|| SIDEBAR ITEMS ||============================== //

const sidebarItems: { items: NavItemType[] } = {
  items: [sidebar],
};

export default sidebarItems;
