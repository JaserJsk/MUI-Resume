import main from './main';
import { NavItemType } from 'types';

// ==============================|| MAIN ITEMS ||============================== //

const mainItems: { items: NavItemType[] } = {
  items: [main],
};

export default mainItems;
